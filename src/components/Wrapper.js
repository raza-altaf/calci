// Wrapper component
const Wrapper = ({ children }) => {
  // Render a div with the className "wrapper" and include children components
  return <div className="wrapper">{children}</div>;
};

// Export the Wrapper component
export default Wrapper;
