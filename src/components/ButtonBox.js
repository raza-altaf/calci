// ButtonBox component
const ButtonBox = ({ children }) => {
  // Render a div with the className "buttonBox" and include children components
  return <div className="buttonBox">{children}</div>;
};

// Export the ButtonBox component
export default ButtonBox;
